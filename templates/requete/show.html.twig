{% extends 'base.html.twig' %}

{% block title %}Requete{% endblock %}

{% block body %}
    <h1>Requete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ requete.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ requete.createdAt ? requete.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ requete.type }}</td>
            </tr>
            <tr>
                <th>Deadline</th>
                <td>{{ requete.deadline ? requete.deadline|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ requete.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ requete.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_requete_index') }}">back to list</a>

    <a href="{{ path('app_requete_edit', {'id': requete.id}) }}">edit</a>

    {{ include('requete/_delete_form.html.twig') }}
{% endblock %}
